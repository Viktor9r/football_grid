import { styled } from '@mui/material/styles';
/* eslint-disable */

export const StyledPageContainer = styled('div')(() => ({
  maxWidth: '1200px',
  margin: 'auto',
  padding: '20px',
  overflow: 'hidden',
}))